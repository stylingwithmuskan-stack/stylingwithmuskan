import fetch from "node-fetch"; // need to import if using node 18+ global fetch, actually node 18 has global fetch.

async function testFetch() {
    // We need to fetch an auth token first. Let's find a user token!
    // I'll just write a script to generate a token for user.
}
testFetch();
